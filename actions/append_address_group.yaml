name: append_address_group_object
pack: fortinet
runner_type: python-script
description: "Append Address Group Details. This pulls an existing address group and appends the
`member` using the update_address_group method. "
enabled: true
entry_point: "append_address_group.py"
parameters:
    name:
        type: "string"
        description: "Name of the Address Group"
        required: True
        position: 0
    member:
        type: "string"
        description: "Address Group member to append"
        required: True
        position: 1

